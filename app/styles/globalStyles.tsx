import { StyleSheet, StatusBar} from 'react-native';

export const globalContainer = StyleSheet.create({
    container: {
        height: '100%',
        flex:1,
        position:'relative',
        padding: 20,
    }
});